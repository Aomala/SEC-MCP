# ═══════════════════════════════════════════════════════════════
#  SEC Terminal — Environment Variables
# ═══════════════════════════════════════════════════════════════

# Required: Your identity for SEC EDGAR API requests (name + email)
# SEC requires this header on all API calls for rate-limiting purposes
EDGAR_IDENTITY="Your Name your.email@domain.com"

# Optional: Claude API key for AI-powered financial summaries
# Get one at https://console.anthropic.com/
# ANTHROPIC_API_KEY="sk-ant-..."

# Optional: MongoDB URI for persistent filing cache
# Without this, the app still works but doesn't cache historical data
# MONGODB_URI="mongodb+srv://user:pass@cluster.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"

# Optional: Server port (Railway sets PORT automatically)
# PORT=8877

# Optional: Override default NLP models (only used locally, not on Railway)
# SENTIMENT_MODEL="ProsusAI/finbert"
# SUMMARIZATION_MODEL="facebook/bart-large-cnn"
# NER_MODEL="dslim/bert-base-NER"

# Optional: Chunking parameters for NLP processing
# MAX_CHUNK_TOKENS=512
# CHUNK_OVERLAP_TOKENS=128
